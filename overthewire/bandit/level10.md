# level: 10
username: bandit10

## notes:
* had to decode a base64 encoding 
* base64 encoding uses 64 characters to encode binary data
* used command base64 -d to decode
* **solution:** `base64 -d data.txt`

## things i didn't know before this level:
* what is base64
* how to decode base64 encoding
